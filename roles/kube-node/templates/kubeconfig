apiVersion: v1
kind: Config
preferences: {}
clusters:
- name: default
  cluster:
    server: https://{{ kube_apiserver_vip }}:{{ kube_apiserver_vip_port }}
    certificate-authority-data: {{ ca_crt_data.stdout }}
users:
- name: kubernetes-admin
  user:
      client-certificate-data: {{ client_crt_data.stdout }}
      client-key-data: {{ client_key_data.stdout }}
contexts:
- context:
    cluster: default
    user: kubernetes-admin
  name: default
current-context: default


copy_list:
  - { src: "{{ cache_dir }}/kubernetes/kube-controller-manager",  dest: "{{ root_dir }}/usr/bin", mode: "0755" }

templates:
  - { src: "controller-manager",              dest: "{{ kube_config_dir }}",              mode: "0644" }
  - { src: "kube-controller-manager.service", dest: "{{ root_dir }}/etc/systemd/system",  mode: "0644" }

start_command:
  - ln -sf {{ root_dir }}/etc/systemd/system/kube-controller-manager.service /etc/systemd/system
  - systemctl daemon-reload
  - systemctl restart kube-controller-manager
  - systemctl enable kube-controller-manager

check_app:
  - systemctl status kube-controller-manager

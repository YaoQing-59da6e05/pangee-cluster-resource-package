- hosts: kube_control_plane,kube_node,etcd
  gather_facts: true
  roles:
    - kube-node-start

- hosts:
    - kube_control_plane
  gather_facts: true
  roles:
    - role: kube-post-start
      when: inventory_hostname == (groups['kube_control_plane'])[0]
